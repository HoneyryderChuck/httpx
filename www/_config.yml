title: HTTPX 
description: A client library for making HTTP requests from Ruby.
authorurl: https://gitlab.com/honeyryderchuck
repourl: https://gitlab.com/honeyryderchuck/httpx
url: honeyryderchuck.gitlab.io/httpx
markdown: kramdown
destination: build
plugins:
  - jemoji
exclude:
  - Gemfile
  - Gemfile.lock
defaults:
  -
    scope:
      type: pages
    values:
      layout: default
